# FastAPI backend — HTTP-слой над db.mongo + scorer.service
# Установка из корня проекта:  pip install -r api/requirements.txt
# (pydantic v2 ставится зависимостью fastapi)

fastapi>=0.110
uvicorn[standard]>=0.27

# Для герметичного теста tests/test_api.py (TestClient):
#   pip install httpx
